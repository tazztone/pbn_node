[pytest]
pythonpath =
testpaths = .
markers =
    unit: Fast unit tests for individual components
    integration: Integration tests for the full pipeline and nodes
    quality: Visual and numerical quality metrics for refinement loops
addopts = -v --tb=short
